{% extends 'baseAdmin.html.twig' %}

{% block title %}University{% endblock %}

{% block body %}
<div class="content-wrapper">
    <h1>University</h1>

    <table id="example1" class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ university.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ university.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_university_index') }}" class="btn btn-info">Retour</a>

    <a href="{{ path('app_university_edit', {'id': university.id}) }}"class="btn btn-success">edit</a>

    {{ include('admin/university/_delete_form.html.twig') }}
</div>    
{% endblock %}
