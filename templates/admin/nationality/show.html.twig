{% extends 'baseAdmin.html.twig' %}

{% block title %}Nationality{% endblock %}

{% block body %}
<div class="content-wrapper">
    <h1>Nationality</h1>

    <table id="example1" class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nationality.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ nationality.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nationality_index') }}" class="btn btn-info">Retour</a>

    <a href="{{ path('app_nationality_edit', {'id': nationality.id}) }}"class="btn btn-success">edit</a>

    {{ include('admin/nationality/_delete_form.html.twig') }}
 </div>   
{% endblock %}
