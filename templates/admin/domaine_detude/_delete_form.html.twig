<form method="post" action="{{ path('app_domaine_detude_delete', {'id': domaine_detude.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ domaine_detude.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
