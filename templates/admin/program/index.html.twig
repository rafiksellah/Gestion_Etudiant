{% extends 'baseAdmin.html.twig' %}

{% block title %}Program index{% endblock %}

{% block body %}
<div class="content-wrapper">
    <h1>Program index</h1>

    <table id="example1" class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for program in programs %}
            <tr>
                <td>{{ program.id }}</td>
                <td>{{ program.name }}</td>
                <td>
                    <a href="{{ path('app_program_show', {'id': program.id}) }}"class="btn btn-primary">show</a>
                    <a href="{{ path('app_program_edit', {'id': program.id}) }}"class="btn btn-success">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_program_new') }}"class="btn btn-primary">Create new</a>
</div>    
{% endblock %}
