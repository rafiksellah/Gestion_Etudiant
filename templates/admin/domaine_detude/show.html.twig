{% extends 'baseAdmin.html.twig' %}

{% block title %}DomaineDetude{% endblock %}

{% block body %}
<div class="content-wrapper">
    <h1>DomaineDetude</h1>

    <table id="example1" class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domaine_detude.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ domaine_detude.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_domaine_detude_index') }}" class="btn btn-info">Retour</a>

    <a href="{{ path('app_domaine_detude_edit', {'id': domaine_detude.id}) }}"class="btn btn-success">edit</a>

    {{ include('admin/domaine_detude/_delete_form.html.twig') }}
</div>    
{% endblock %}
