{% extends 'baseAdmin.html.twig' %}

{% block title %}Country{% endblock %}

{% block body %}
<div class="content-wrapper"> 
    <h1>Country</h1>

    <table id="example1" class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ country.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ country.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_country_index') }}" class="btn btn-info">Retour</a>

    <a href="{{ path('app_country_edit', {'id': country.id}) }}"class="btn btn-success">edit</a>

    {{ include('admin/country/_delete_form.html.twig') }}
</div>    
{% endblock %}
